# إعداد ممحاة X

يستخدم التطبيق **X OAuth 2.0 Authorization Code with PKCE**. لا يطلب كلمة مرور المستخدم ولا يخزنها. بعد الموافقة، يُحفظ رمز الوصول داخل Cookie مشفّر على الخادم فقط.

## Secrets المطلوبة

```env
X_CLIENT_ID=معرّف التطبيق من لوحة X Developer
X_CLIENT_SECRET=سر التطبيق إن كان نوع التطبيق confidential
X_REDIRECT_URI=https://your-domain.example/api/x/callback
```

يجب أن يطابق `X_REDIRECT_URI` عنوان Callback المسجل حرفيًا داخل إعدادات تطبيق X. في المعاينة، استخدم عنوان المعاينة الحالي مع المسار `/api/x/callback` إذا كانت لوحة X تسمح به.

## الصلاحيات

يطلب التطبيق: `tweet.read`, `tweet.write`, `like.read`, `like.write`, `users.read`, و`offline.access`. يحتاج الحذف إلى `tweet.write`، ويحتاج قراءة الإعجابات إلى `like.read`. توفر هذه العمليات يعتمد على خطة X API الحالية وحساب المطور.

## سلوك الحذف

1. يربط المستخدم حسابه عبر OAuth.
2. يضغط **فحص الحساب** لجلب المنشورات والردود.
3. يحدد العناصر أو يختار أول 10/100/1000 عنصرًا.
4. تظهر نافذة تأكيد وتطلب كتابة `حذف`.
5. يرسل الخادم طلبات DELETE إلى X، ولا ينفذ الحذف تلقائيًا أو في الخلفية.

الحذف لا يمكن التراجع عنه عادةً. الإعجابات تُعرض كإحصائية في النسخة الحالية، أما حذف الإعجابات فيحتاج مسارًا منفصلًا عبر `DELETE /2/users/:id/likes/:tweet_id` عند توفر الصلاحية والخطة المناسبة.
